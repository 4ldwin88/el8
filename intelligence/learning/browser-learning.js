// Browser bridge for durable, evidence-backed learning after Review.
export function recordReviewLearning(state,{outcomeEvidence,review,at=new Date().toISOString()}={}){if(!state||!Number.isInteger(state.revision))throw new Error('canonical Member State required');const outcome=outcomeEvidence?.outcome||outcomeEvidence;if(!outcome?.outcomeId)throw new Error('canonical Outcome required');const refs=[...(outcome.evidenceRefs||[])];if(!refs.length)throw new Error('learning requires Outcome evidence references');if(!review?.decision)throw new Error('Review decision required');const next=structuredClone(state),item={id:`learning:${outcome.outcomeId}:${review.decision}`,type:'INTERVENTION_OUTCOME',interventionId:outcome.interventionId,problemId:outcome.concernId||null,outcomeId:outcome.outcomeId,reviewDecision:review.decision,reviewReason:review.reason||null,benefitDirection:outcome.benefitDirection??null,adherence:outcome.adherence??null,burden:outcome.burden??null,measurementSufficient:Boolean(outcome.measurementSufficient),evidenceRefs:refs,observationRefs:[...(outcome.observationRefs||[])],learnedAt:at};const i=(next.learning||[]).findIndex(x=>x.id===item.id);if(i<0)next.learning=[...(next.learning||[]),item];else next.learning[i]={...next.learning[i],...item};next.revision=state.revision+1;next.updatedAt=at;next.history=[...(next.history||[]),{revision:next.revision,previousRevision:state.revision,type:'LEARNING_RECORDED',at,source:'review:outcome-learning'}];return next;}
export function learningForProblem(state,problemId){return(state?.learning||[]).filter(x=>x.problemId===problemId).sort((a,b)=>String(b.learnedAt).localeCompare(String(a.learnedAt)))}
