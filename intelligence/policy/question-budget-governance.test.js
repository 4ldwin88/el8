import assert from'node:assert/strict';import{dimensionQuestionCounts,unresolvedDimensions,eligibleCandidates,breadthDimensions,shouldContinueBreadth,chooseDiscoveryDimension}from'./question-budget-governance.js';const h={Physical:{state:'candidate',confidence:.8},Financial:{state:'candidate',confidence:.6},Social:{state:'unknown'}};let t=[{stage:'confirmation',dimension:'Physical'},{stage:'confirmation',dimension:'Physical'}];assert.equal(dimensionQuestionCounts(t).Physical.confirmation,2);assert.deepEqual(unresolvedDimensions(h,t),['Physical']);assert.deepEqual(eligibleCandidates(h,t),['Financial']);assert.equal(breadthDimensions(t).size,1);assert.equal(shouldContinueBreadth(h,t),true);assert.equal(chooseDiscoveryDimension(['Physical','Emotional','Social'],{Physical:{observed:true}},t),'Emotional');t=[{stage:'confirmation',dimension:'Physical'},{stage:'confirmation',dimension:'Financial'},{stage:'discovery',dimension:'Social'},{stage:'discovery',dimension:'Emotional'}];assert.equal(shouldContinueBreadth(h,t),false);console.log('question budget governance tests passed');