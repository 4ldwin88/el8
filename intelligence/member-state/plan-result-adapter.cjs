'use strict';
const {applyMemberStateUpdate}=require('./member-state-update');
function reviewDueAt(createdAt,reviewDays){if(!createdAt||!Number.isFinite(Number(reviewDays)))return null;const d=new Date(createdAt);if(Number.isNaN(d.getTime()))return null;d.setUTCDate(d.getUTCDate()+Number(reviewDays));return d.toISOString()}
function canonicalPlanResultToUpdate(plan,{source='planning:canonical',at=plan?.createdAt||new Date().toISOString()}={}){if(!plan||!Number.isInteger(plan.memberStateRevision))throw new Error('canonical plan result with memberStateRevision is required');if(plan.status!=='active')throw new Error('only active canonical plan results may update activePlan');const active=Array.isArray(plan.active)?plan.active:[];if(!active.length)throw new Error('active canonical plan result requires interventions');return{type:'PLAN_UPDATED',source,at,expectedRevision:plan.memberStateRevision,payload:{planId:`plan:r${plan.memberStateRevision}`,status:'ACTIVE',activatedAt:at,reviewDueAt:reviewDueAt(at,plan.reviewDays),interventions:active.map(i=>({id:i.intervention_id||i.id,priorityId:i.priorityId,problemId:i.problemId,status:'ACCEPTED',title:i.title,purpose:i.purpose,selectionMechanism:i.selectionMechanism||null,measurement:{...(i.measurement||{})},reviewRule:{...(i.reviewRule||{})},burden:{...(i.burden||{})},evidenceRefs:[...(i.evidenceRefs||[])]}))}}}
function applyCanonicalPlanResult(state,plan,options={}){return applyMemberStateUpdate(state,canonicalPlanResultToUpdate(plan,options))}
module.exports={canonicalPlanResultToUpdate,applyCanonicalPlanResult};
