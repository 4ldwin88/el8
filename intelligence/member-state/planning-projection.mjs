export function projectPlanningInput(state){if(!state||!Number.isInteger(state.revision))throw new Error('canonical Member State required');const accepted=(state.priorities||[]).filter(p=>p.status==='ACCEPTED'),acceptedIds=new Set(accepted.map(p=>p.id)),priorityByProblem=new Map(accepted.filter(p=>p.problemId).map(p=>[p.problemId,p.id]));const problems=(state.problems||[]).map(problem=>{const priorityId=problem.priorityId||priorityByProblem.get(problem.id)||null;if(!priorityId||!acceptedIds.has(priorityId))return null;return{problemId:problem.id,priorityId,status:problem.status||null,evidenceRefs:[...(problem.evidenceRefs||[])]}}).filter(Boolean);return{memberStateRevision:state.revision,confirmedPriorityIds:accepted.map(p=>p.id),problems,constraints:{capacity:state.engagementBurden?.capacity||'medium',throttle:{active:Boolean(state.engagementBurden?.throttle?.active),reasonCodes:[...(state.engagementBurden?.throttle?.reasonCodes||[])]},safety:{disposition:state.safety?.disposition||'ORDINARY_FLOW',unresolvedConstraints:[...(state.safety?.unresolvedConstraints||[])]}}}}
