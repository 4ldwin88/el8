import test from'node:test';import assert from'node:assert/strict';import{scoreActionRecommendation,confidenceBand}from'./recommendation-scoring.js';
test('high fit and evidence produce high recommendation strength',()=>{const s=scoreActionRecommendation({fit:{mechanismMatch:1,feasibility:1,memberPreference:1,actionability:1,priorLearning:1,decisionValue:1},confidence:{evidenceStrength:1,discrimination:1,recencyRelevance:1,sufficiency:1,corroboration:1}});assert.equal(s.actionFit,1);assert.equal(s.recommendationConfidence,1);assert.equal(s.recommendationStrength,1);assert.equal(s.confidenceBand,'high')});
test('contradiction cannot be averaged away',()=>{const s=scoreActionRecommendation({fit:{mechanismMatch:1,feasibility:1,memberPreference:1,actionability:1,priorLearning:1,decisionValue:1},confidence:{evidenceStrength:1,discrimination:1,recencyRelevance:1,sufficiency:1,corroboration:1},materialContradiction:.8});assert.ok(s.recommendationConfidence<.4);assert.equal(s.confidenceBand,'insufficient')});
test('failed hard gate excludes recommendation regardless score',()=>{const s=scoreActionRecommendation({fit:{mechanismMatch:1,feasibility:1,memberPreference:1,actionability:1,priorLearning:1,decisionValue:1},confidence:{evidenceStrength:1,discrimination:1,recencyRelevance:1,sufficiency:1,corroboration:1},hardGates:{safety:true,eligibility:false,requiredEvidence:true}});assert.equal(s.eligible,false);assert.deepEqual(s.hardGateFailures,['eligibility'])});
test('confidence bands are canonical calibration bands',()=>{assert.equal(confidenceBand(.39),'insufficient');assert.equal(confidenceBand(.4),'limited');assert.equal(confidenceBand(.6),'moderate');assert.equal(confidenceBand(.8),'high')});
