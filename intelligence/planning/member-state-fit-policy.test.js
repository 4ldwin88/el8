import test from'node:test';import assert from'node:assert/strict';import{deriveMemberStatePlanningPolicy}from'./member-state-fit-policy.js';
const now='2026-09-02T19:00:00Z';
test('low capacity bounds portfolio burden',()=>{const p=deriveMemberStatePlanningPolicy({planningContext:{global:{memberStateFit:{capacity:'low',manageability:'unknown',engagementSignals:{}}}}},{now});assert.equal(p.burdenBudget,1);assert.equal(p.actionCeiling,1)});
test('current evidence-backed burden tolerance bounds portfolio burden',()=>{const p=deriveMemberStatePlanningPolicy({planningContext:{global:{memberStateFit:{capacity:'unknown',manageability:'unknown',engagementSignals:{burdenTolerance:{key:'burdenTolerance',value:'low',status:'current',confidence:'moderate',observedAt:'2026-09-01T19:00:00Z'}}}}}},{now});assert.equal(p.burdenBudget,1);assert.equal(p.actionCeiling,1)});
test('stale engagement learning cannot constrain a new plan',()=>{const p=deriveMemberStatePlanningPolicy({planningContext:{global:{memberStateFit:{capacity:'unknown',manageability:'unknown',engagementSignals:{burdenTolerance:{key:'burdenTolerance',value:'low',status:'current',confidence:'moderate',observedAt:'2026-01-01T00:00:00Z'}}}}}},{now});assert.equal(p.burdenBudget,null);assert.equal(p.actionCeiling,3);assert.ok(p.diagnostics.reasons.includes('member_state_burden_signal_not_decision_usable'))});
test('explicit caller budget remains authoritative over derived fit policy',()=>{const p=deriveMemberStatePlanningPolicy({planningContext:{global:{memberStateFit:{capacity:'low'}}}},{burdenBudget:2,actionCeiling:2,now});assert.equal(p.burdenBudget,2);assert.equal(p.actionCeiling,2)});
