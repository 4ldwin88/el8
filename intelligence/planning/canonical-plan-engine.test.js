import assert from 'node:assert/strict';import test from'node:test';import{buildCanonicalPlan}from'./canonical-plan-engine.js';
const base={memberStateRevision:5,confirmedPriorityIds:['priority:poor_sleep'],memberChoice:{mode:'EXPLICIT_ACCEPTANCE'},problems:[{priorityId:'priority:poor_sleep',problemId:'problem:poor_sleep',evidenceRefs:['e1'],priorLearning:[]}],constraints:{profile:[],capacity:'medium',manageability:'manageable',throttle:{active:false},safety:{disposition:'ORDINARY_FLOW'}}};
const sleepEvidence={'baseline.sleep_pattern':'Timing changes a lot'};
const multi={...base,confirmedPriorityIds:['priority:low_activity','priority:poor_sleep'],problems:[{priorityId:'priority:low_activity',problemId:'problem:low_activity',evidenceRefs:['e1'],priorLearning:[]},{priorityId:'priority:poor_sleep',problemId:'problem:poor_sleep',evidenceRefs:['e2'],priorLearning:[]}]};const multiEvidence={'baseline.activity_level':'Some intentional activity','baseline.sleep_pattern':'Timing changes a lot'};
test('accepted focus deepens for intervention-selection evidence rather than rechecking problem sufficiency',()=>{const r=buildCanonicalPlan(base);assert.equal(r.status,'deepen');assert.equal(r.reason,'selection_evidence_required');assert.ok(r.selectionDeepening.requirements.some(x=>x.evidenceKey==='baseline.sleep_pattern'))});
test('selection evidence unlocks registry intervention choice without numeric Discovery confidence gate',()=>{const r=buildCanonicalPlan({...base,problems:[{...base.problems[0],confidence:0}]},{selectionEvidence:sleepEvidence});assert.equal(r.status,'active');assert.equal(r.memberStateRevision,5);assert.equal(r.decisionTrace.confirmedPriorityIds[0],'priority:poor_sleep');assert.equal('score'in r.active[0],false)});
test('multi-priority plan covers each accepted priority before adding second component',()=>{const r=buildCanonicalPlan(multi,{selectionEvidence:multiEvidence});assert.equal(r.status,'active');assert.equal(r.active.length,2);assert.deepEqual(new Set(r.active.map(x=>x.priorityId)),new Set(multi.confirmedPriorityIds))});
test('adapting one priority preserves the unaffected priority intervention',()=>{const first=buildCanonicalPlan(multi,{selectionEvidence:multiEvidence});const activity=first.active.find(x=>x.priorityId==='priority:low_activity'),sleep=first.active.find(x=>x.priorityId==='priority:poor_sleep');assert.ok(activity&&sleep);const next=buildCanonicalPlan(multi,{selectionEvidence:multiEvidence,rejectedInterventionIds:[activity.intervention_id],previousInterventionIds:[activity.intervention_id],adaptationConstraint:'different_intervention'});assert.equal(next.status,'active');const nextActivity=next.active.find(x=>x.priorityId==='priority:low_activity'),nextSleep=next.active.find(x=>x.priorityId==='priority:poor_sleep');assert.ok(nextActivity&&nextSleep);assert.notEqual(nextActivity.intervention_id,activity.intervention_id);assert.equal(nextSleep.intervention_id,sleep.intervention_id)});
test('adapting second priority does not mutate first priority intervention',()=>{const first=buildCanonicalPlan(multi,{selectionEvidence:multiEvidence});const activity=first.active.find(x=>x.priorityId==='priority:low_activity'),sleep=first.active.find(x=>x.priorityId==='priority:poor_sleep');const next=buildCanonicalPlan(multi,{selectionEvidence:multiEvidence,rejectedInterventionIds:[sleep.intervention_id],previousInterventionIds:[sleep.intervention_id],adaptationConstraint:'different_intervention'});assert.equal(next.status,'active');assert.equal(next.active.find(x=>x.priorityId==='priority:low_activity').intervention_id,activity.intervention_id);assert.notEqual(next.active.find(x=>x.priorityId==='priority:poor_sleep').intervention_id,sleep.intervention_id)});
test('capacity throttle prevents new active intervention creation',()=>{const r=buildCanonicalPlan({...base,constraints:{...base.constraints,throttle:{active:true}}},{selectionEvidence:sleepEvidence});assert.equal(r.status,'observe');assert.equal(r.reason,'capacity_throttle');assert.deepEqual(r.active,[])});
test('capacity throttle still blocks replacement during adaptation',()=>{const first=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence});const rejected=first.active[0].intervention_id;const r=buildCanonicalPlan({...base,constraints:{...base.constraints,throttle:{active:true}}},{selectionEvidence:sleepEvidence,rejectedInterventionIds:[rejected],previousInterventionIds:[rejected],adaptationConstraint:'different_intervention'});assert.equal(r.status,'observe');assert.equal(r.reason,'capacity_throttle');assert.deepEqual(r.active,[])});
test('Safety override precedes selection deepening and ordinary Planning',()=>{const r=buildCanonicalPlan({...base,constraints:{...base.constraints,safety:{disposition:'PAUSE_ORDINARY_FLOW'}}});assert.equal(r.status,'escalate');assert.equal(r.reason,'safety_override')});
test('Safety interruption blocks replacement even when an alternative intervention exists',()=>{const first=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence});const rejected=first.active[0].intervention_id;const r=buildCanonicalPlan({...base,constraints:{...base.constraints,safety:{disposition:'PAUSE_ORDINARY_FLOW'}}},{selectionEvidence:sleepEvidence,rejectedInterventionIds:[rejected],previousInterventionIds:[rejected],adaptationConstraint:'different_intervention'});assert.equal(r.status,'escalate');assert.equal(r.reason,'safety_override');assert.deepEqual(r.active,[])});
test('Planning requires member-confirmed priority focus',()=>{const r=buildCanonicalPlan({...base,confirmedPriorityIds:[],problems:[]});assert.equal(r.reason,'member_priority_required')});
test('reassessment changing accepted priority prevents stale priority replanning',()=>{const changed={...base,memberStateRevision:6,confirmedPriorityIds:['priority:low_activity'],problems:[{priorityId:'priority:low_activity',problemId:'problem:low_activity',evidenceRefs:['e2'],priorLearning:[]} ]};const r=buildCanonicalPlan(changed,{selectionEvidence:{'baseline.activity_level':'Some intentional activity'}});assert.equal(r.status,'active');assert.deepEqual(r.decisionTrace.confirmedPriorityIds,['priority:low_activity']);assert.ok(r.active.every(x=>x.priorityId==='priority:low_activity'));assert.equal(r.active.some(x=>x.priorityId==='priority:poor_sleep'),false)});
test('rejected interventions cannot be selected again',()=>{const first=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence});assert.equal(first.status,'active');const rejected=first.active[0].intervention_id;const next=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence,rejectedInterventionIds:[rejected],adaptationConstraint:'different_intervention',previousInterventionIds:[rejected]});assert.equal(next.active.some(x=>x.intervention_id===rejected),false)});
test('no eligible replacement returns observation instead of resurrecting rejected intervention',()=>{const first=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence});const allIds=[...first.active,...first.backlog].map(x=>x.intervention_id);const r=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence,rejectedInterventionIds:allIds,previousInterventionIds:allIds,adaptationConstraint:'different_intervention'});assert.equal(r.status,'observe');assert.equal(r.reason,'no_eligible_registry_intervention');assert.deepEqual(r.active,[])});
test('canonical plan preserves measurement and review metadata for closed-loop evaluation',()=>{const r=buildCanonicalPlan(base,{selectionEvidence:sleepEvidence});assert.equal(r.status,'active');assert.ok('measurement'in r.active[0]);assert.ok('reviewRule'in r.active[0]);assert.ok('burden'in r.active[0])});
test('low capacity limits active plan to one component',()=>{const r=buildCanonicalPlan({...base,constraints:{...base.constraints,capacity:'low'}},{selectionEvidence:sleepEvidence});assert.equal(r.active.length,1)});
