import assert from'node:assert/strict';import{buildCanonicalPlan}from'./canonical-plan-engine.js';const focus=id=>({constructId:id,decision:'accepted',decidedAt:'2026-08-30T16:30:00Z'});const input={memberStateRevision:8,focuses:[focus('SLEEP_QUALITY'),focus('ACTIVITY_LEVEL'),focus('FINANCIAL_STRAIN')],evidenceRefs:[],constraintRefs:['c1'],safetyDisposition:'ordinary_flow'};
let plan=buildCanonicalPlan(input,{now:'2026-08-30T16:31:00Z'});assert.equal(plan.schemaVersion,'2.0.0');assert.equal(plan.status,'proposed');assert.equal(plan.focusIds.length,3);assert.equal(plan.proposedActions.length,3);assert.deepEqual(plan.uncoveredFocusIds,[]);assert.equal(plan.burden.budget,'unknown');assert.ok(plan.proposedActions.every(a=>a.actionScope==='construct'));assert.ok(plan.proposedActions.every(a=>a.trackingRequirement&&a.iconKey&&a.permittedRationaleClaim));assert.equal(plan.proposedActions.some(a=>a.intervention_id||a.problemId||a.problem_id||a.scope),false);
const tight=buildCanonicalPlan(input,{burdenBudget:1,now:'2026-08-30T16:31:00Z'});assert.ok(tight.proposedActions.length<3);assert.ok(tight.uncoveredFocusIds.length>0);assert.equal(tight.burden.budget,1);
const preferred=buildCanonicalPlan({memberStateRevision:1,focuses:[focus('ACTIVITY_LEVEL')],evidenceRefs:[],constraintRefs:[],safetyDisposition:'ordinary_flow'},{preferredActionIds:['PHY-A06']});assert.equal(preferred.proposedActions[0].actionId,'PHY-A06');
const rejected=buildCanonicalPlan({memberStateRevision:1,focuses:[focus('ACTIVITY_LEVEL')],evidenceRefs:[],constraintRefs:[],safetyDisposition:'ordinary_flow'},{rejectedActionIds:['PHY-A06','PHY-A07']});assert.equal(rejected.proposedActions.some(a=>['PHY-A06','PHY-A07'].includes(a.actionId)),false);
const safety=buildCanonicalPlan({...input,safetyDisposition:'pause_ordinary_flow'});assert.equal(safety.status,'blocked');assert.equal(safety.reason,'safety_override');assert.throws(()=>buildCanonicalPlan({...input,focuses:[{...focus('SLEEP_QUALITY'),decision:'rejected'}]}),/member-accepted Focus/);
const planLevel=buildCanonicalPlan({memberStateRevision:2,focuses:[focus('ACTIVITY_LEVEL')],evidenceRefs:[],constraintRefs:[],safetyDisposition:'ordinary_flow'},{planningConditions:['reduce_simultaneous_demands'],preferredActionIds:['XDM-A01']});const xdm=planLevel.proposedActions.find(a=>a.actionId==='XDM-A01');assert.ok(xdm);assert.equal(xdm.actionScope,'plan');assert.deepEqual(xdm.focusIds,[]);assert.ok(planLevel.proposedActions.some(a=>a.actionScope==='construct'&&a.focusIds.includes('ACTIVITY_LEVEL')));
console.log('Canonical Planning v2 uses confirmed Focus, Action Registry, explicit plan conditions, burden and no fixed count semantics');
