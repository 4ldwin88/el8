import assert from'node:assert/strict';import{createHierarchicalState,applySubdimensionEvidence,deriveDimensions,dimensionEvidenceCounts}from'./hierarchical-state.js';let s=createHierarchicalState();s=applySubdimensionEvidence(s,{'Financial.IncomeAdequacy':.4});let d=deriveDimensions(s);assert.ok(d.Financial>.12);assert.equal(d.Physical,.12);s=applySubdimensionEvidence(s,{'Financial.DebtBurden':.35});d=deriveDimensions(s);assert.ok(d.Financial>.3);assert.equal(dimensionEvidenceCounts(s).Financial,2);console.log('hierarchical state tests passed');