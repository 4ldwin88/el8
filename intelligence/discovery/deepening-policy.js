export function selectDecisionCriticalDeepening({rankedHypotheses=[],questionBank=[],askedIds=[],limit=1}={}){
 const asked=new Set(askedIds);for(const hypothesis of rankedHypotheses){if(hypothesis.uncertainty<.25)continue;const candidates=questionBank.filter(q=>!asked.has(q.id)&&(q.constructId===hypothesis.constructId||(q.constructIds??[]).includes(hypothesis.constructId))&&!['orientation','orientation-baseline','baseline-discriminator','general'].includes(q.role));const ordered=[...candidates].sort((a,b)=>(b.specificityLevel??0)-(a.specificityLevel??0));if(ordered.length)return Object.freeze({reason:'decision-critical-discriminator',hypothesis,questions:Object.freeze(ordered.slice(0,limit))});}return Object.freeze({reason:'no-decision-critical-deepening',hypothesis:null,questions:Object.freeze([])});
}