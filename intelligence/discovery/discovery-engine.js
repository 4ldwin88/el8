import BANK,{observationsForAnswer,constructsForAnswer,safetyContextForAnswer,openingPathForAnswer,baselineCoverageForAnswer} from './observationNormalizer.js';
import {createDiscoverySession,appendObservation,nextOrientationStep,markTriaged,deriveStates,setResolution,mergeFacts,activateConstructs} from './orientation-session.js';
import {createDiscoveryOrchestration,nextDiscoveryDecision} from './discovery-orchestrator.js';
import {safetyGate} from '../safety/gate.js';
function orchestrationAreas(s){return Object.entries(s.baselineCoverage||{}).map(([dimension,value])=>({dimensionId:dimension,state:value?.state??'unknown'}))}
function driverSelections(s){return {...(s.driverSelections||{})}}
function severityResponses(s){return {...(s.severityResponses||{})}}
function relationshipEvidence(s){return {...(s.relationshipEvidence||{})}}
function orchestrate(s){return createDiscoveryOrchestration({memberStateRevision:s.memberStateRevision??null,areas:orchestrationAreas(s),constructStates:deriveStates(s),questionBank:s.questionBank??BANK,relationshipEvidence:relationshipEvidence(s),driverSelections:driverSelections(s),severityResponses:severityResponses(s),unresolvedRequirements:s.unresolvedRequirements??[]})}
export function session(options={}){const s=createDiscoverySession({constructIds:options.constructIds??[],questionBank:options.questionBank??BANK,labels:options.labels??{},outerGuardrail:options.outerGuardrail??null,facts:options.facts??{}});s.assessmentStart=Date.now();s.questionTimings=[];s.baselineCoverage={...(options.baselineCoverage||{})};s.driverSelections={...(options.driverSelections||{})};s.severityResponses={...(options.severityResponses||{})};s.relationshipEvidence={...(options.relationshipEvidence||{})};s.unresolvedRequirements=[...(options.unresolvedRequirements||[])];s.memberStateRevision=options.memberStateRevision??null;s.safetyContextualSignals={...(options.safetyContextualSignals||{})};s.safetyConfirmation=options.safetyConfirmation||null;s.safety=safetyGate({stage:'discovery',contextualSignals:s.safetyContextualSignals,confirmation:s.safetyConfirmation,constructRefs:s.constructIds});return s}
export function setSafetyContext(s,contextualSignals={},confirmation=null){s.safetyContextualSignals={...(s.safetyContextualSignals||{}),...contextualSignals};if(confirmation!==undefined)s.safetyConfirmation=confirmation;const refs=(s.observationLog||[]).map(x=>x.id).filter(Boolean);s.safety=safetyGate({stage:'discovery',contextualSignals:s.safetyContextualSignals,confirmation:s.safetyConfirmation,observationRefs:refs,evidenceRefs:refs,constructRefs:s.constructIds});return s}
export function next(s){if(s.safety?.pauseOrdinaryFlow)return{type:'safety',safety:s.safety,states:deriveStates(s)};if(s.phase==='orient'||s.phase==='narrow'||s.constructIds.length===0){const orientation=nextOrientationStep(s);if(orientation)return orientation}const orchestration=orchestrate(s);s.orchestration=orchestration;const decision=nextDiscoveryDecision(orchestration);if(decision.type==='question'){s.asked=[...new Set([...s.asked,decision.question.id])];s.questionsAsked++;return{...decision,states:deriveStates(s)}}if(decision.type==='handoff'){s.phase='ready_for_prioritization';s.incomplete=false;return{type:'finish',stop:{reason:decision.reason,incomplete:false,candidateIds:orchestration.driverGraph.nodes.filter(x=>x.status==='supported'&&!x.excluded).map(x=>x.constructId),boundedUncertainty:false},states:deriveStates(s),orchestration}}return{...decision,states:deriveStates(s),orchestration}}
export function recordQuestionTiming(s,questionId,startedAt,completedAt=Date.now()){s.questionTimings.push({questionId,startedAt,completedAt,durationSeconds:Math.max(0,(completedAt-startedAt)/1000)});return s}
function applyGovernedFocusedSufficiency(s,question){if(!question?.maySatisfyFocusedEvidence)return;const targets=question.constructIds?.length?question.constructIds:[question.constructId].filter(Boolean),states=deriveStates(s);for(const constructId of targets){const state=states.find(x=>x.constructId===constructId);if(state&&!state.excluded&&state.qualitativeConfidence==='WELL_SUPPORTED')setResolution(s,constructId,'sufficient',{driverKnown:true})}}
export function answer(s,question,answerIds){const routed=constructsForAnswer(question,answerIds);if(routed.length)activateConstructs(s,routed);const openingPath=openingPathForAnswer(question,answerIds);if(openingPath)s.openingPath=openingPath;for(const baseline of baselineCoverageForAnswer(question,answerIds))s.baselineCoverage[baseline.dimension]={state:baseline.state,effectId:baseline.effectId,questionId:question.id,timestamp:Date.now()};for(const o of observationsForAnswer(question,answerIds))appendObservation(s,o);applyGovernedFocusedSufficiency(s,question);const safety=safetyContextForAnswer(question,answerIds);if(safety.requiresImmediacyClarification)s.safetyRequiresImmediacyClarification=true;return setSafetyContext(s,safety.contextualSignals,s.safetyConfirmation)}
export function seedFacts(s,facts={}){return mergeFacts(s,facts)}
export function setDriverSelections(s,selections={}){s.driverSelections={...(s.driverSelections||{}),...selections};const accepted=Object.entries(selections).filter(([,value])=>['accept','accepted','yes','supported'].includes(String(value).toLowerCase())).map(([constructId])=>constructId);if(accepted.length)activateConstructs(s,accepted);return s}
export function setSeverityResponses(s,responses={}){s.severityResponses={...(s.severityResponses||{}),...responses};return s}
export function setRelationshipEvidence(s,evidence={}){s.relationshipEvidence={...(s.relationshipEvidence||{}),...evidence};return s}
function applyImportance(s,importanceByConstruct={},questionId='TRIAGE'){const now=Date.now();for(const[constructId,value]of Object.entries(importanceByConstruct))if(s.constructIds.includes(constructId)){appendObservation(s,{id:`${questionId}:${constructId}:${now}`,questionId,constructId,answerValue:value,specificityLevel:0,timestamp:now,effects:Object.freeze([{type:'importance',target:constructId,value,sourceType:'direct',temporality:'current'}])});if(questionId==='TRIAGE')setResolution(s,constructId,Number(value)===0?'deferred':'triaged')}markTriaged(s);return s}
export function seedImportance(s,importanceByConstruct={}){return applyImportance(s,importanceByConstruct,'DISCOVERY_IMPORTANCE')}
export function triage(s,importanceByConstruct={}){return applyImportance(s,importanceByConstruct,'TRIAGE')}
export function resolve(s,constructId,resolutionState,opts={}){return setResolution(s,constructId,resolutionState,opts)}
export function complete(s){if(!s.assessmentCompleted)s.assessmentCompleted=Date.now();return s}
export function trace(s){const end=s.assessmentCompleted??Date.now(),duration=Math.max(0,(end-s.assessmentStart)/1000);return{phase:s.phase,openingPath:s.openingPath??null,questionsAsked:s.questionsAsked,asked:[...s.asked],timing:{assessmentStart:new Date(s.assessmentStart).toISOString(),assessmentCompleted:s.assessmentCompleted?new Date(s.assessmentCompleted).toISOString():null,totalDurationSeconds:duration,questionCount:s.questionsAsked,averageSecondsPerQuestion:s.questionTimings.length?s.questionTimings.reduce((a,x)=>a+x.durationSeconds,0)/s.questionTimings.length:null,questionTimings:[...s.questionTimings]},baselineCoverage:{...(s.baselineCoverage||{})},activeConstructs:[...s.constructIds],triaged:s.triaged,incomplete:s.incomplete,states:deriveStates(s),facts:{...(s.facts||{})},observations:[...s.observationLog],orchestration:s.orchestration||null,safety:s.safety||null,safetySignals:s.safety?.contextual?.signals||[],safetyRequiresImmediacyClarification:s.safetyRequiresImmediacyClarification===true}}
export {BANK};export default{BANK,session,next,answer,setSafetyContext,seedFacts,setDriverSelections,setSeverityResponses,setRelationshipEvidence,seedImportance,triage,resolve,recordQuestionTiming,complete,trace};