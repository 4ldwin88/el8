import assert from 'node:assert/strict';
import test from 'node:test';
import {selectNextQuestion} from './question-scheduler.js';
import {stoppingDecision} from './sufficiency.js';
const q=(id,extra={})=>({id,concernId:'c1',eligible:true,...extra});
test('dismissed concern is offered positive path instead of manufactured concern',()=>{const states=[{concernId:'c1',resolutionState:'dismissed'}];const decision=selectNextQuestion({candidates:[q('ordinary'),q('growth',{path:'positive',role:'goal-probe'})],states});assert.equal(decision.question.id,'growth');assert.equal(decision.reason,'positive-goal-path')});
test('dismissed concern stops when no additional positive question remains',()=>{const states=[{concernId:'c1',resolutionState:'dismissed'}];const decision=selectNextQuestion({candidates:[q('ordinary')],states});assert.equal(decision.type,'none');assert.equal(decision.reason,'decision-relevance-sufficient')});
test('unresolved evidence does not qualify a member for the positive path',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const decision=selectNextQuestion({candidates:[q('ordinary',{decisionCritical:true}),q('growth',{path:'positive',role:'goal-probe'})],states});assert.equal(decision.question.id,'ordinary');assert.equal(decision.reason,'required-handoff-evidence')});
test('Safety clarification preempts required evidence and positive questions',()=>{const states=[{concernId:'c1',resolutionState:'unresolved',sufficiencyRequirements:[{id:'current-state',required:true,satisfied:false}]}];const decision=selectNextQuestion({candidates:[q('state',{sufficiencyRequirement:{id:'current-state'}}),q('growth',{path:'positive'}),q('safe',{path:'safety',safetyPriority:100})],states});assert.equal(decision.question.id,'safe');assert.equal(decision.reason,'safety-clarification-path')});
test('ordinary question cannot masquerade as Safety clarification',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const decision=selectNextQuestion({candidates:[q('ordinary',{safetyPriority:100,decisionCritical:true})],states});assert.equal(decision.question.id,'ordinary');assert.equal(decision.reason,'required-handoff-evidence')});
test('legacy numeric information fields alone cannot justify an ordinary question',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const decision=selectNextQuestion({candidates:[q('legacy-score',{expectedUncertaintyReduction:.99,coverageDeficit:1,redundancyPenalty:0})],states});assert.equal(decision.type,'none');assert.equal(decision.reason,'decision-relevance-sufficient')});
test('explicit unresolved clarification remains decision relevant without a numeric score',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const decision=selectNextQuestion({candidates:[q('clarify',{addressesUnresolvedUncertainty:true})],states});assert.equal(decision.question.id,'clarify');assert.equal(decision.reason,'unresolved-clarification')});
test('unresolved evidence contract outranks generic required handoff evidence',()=>{const states=[{concernId:'c1',resolutionState:'unresolved',sufficiencyRequirements:[{id:'current-state',required:true,satisfied:false}]}];const decision=selectNextQuestion({candidates:[q('handoff',{requiredForHandoff:true,burden:0}),q('state',{sufficiencyRequirement:{id:'current-state'},burden:1})],states});assert.equal(decision.question.id,'state');assert.equal(decision.reason,'required-evidence-contract')});
test('scheduler does not use member importance to allocate Discovery questions',()=>{const states=[{concernId:'c1',resolutionState:'unresolved',memberImportanceRank:4},{concernId:'c2',resolutionState:'unresolved',memberImportanceRank:1}];const decision=selectNextQuestion({candidates:[q('higher-importance',{concernId:'c1',addressesUnresolvedUncertainty:true,burden:2}),q('lower-burden',{concernId:'c2',addressesUnresolvedUncertainty:true,burden:1})],states});assert.equal(decision.question.id,'lower-burden');assert.equal('allocation' in decision,false)});
test('lower burden breaks ties between equally necessary questions',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const decision=selectNextQuestion({candidates:[q('higher-burden',{addressesUnresolvedUncertainty:true,burden:3}),q('lower-burden',{addressesUnresolvedUncertainty:true,burden:1})],states});assert.equal(decision.question.id,'lower-burden')});
test('already asked candidate is not selected again',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const asked=q('asked',{addressesUnresolvedUncertainty:true}),fresh=q('fresh',{addressesUnresolvedUncertainty:true});const decision=selectNextQuestion({candidates:[asked,fresh],states,recentQuestions:[asked]});assert.equal(decision.question.id,'fresh')});
test('outer guardrail bounds unresolved ordinary Discovery',()=>{const states=[{concernId:'c1',resolutionState:'unresolved'}];const stop=stoppingDecision({states,questionsAsked:14,outerGuardrail:14});assert.equal(stop.stop,true);assert.equal(stop.reason,'outer-guardrail');assert.equal(stop.incomplete,true)});
