import {deriveAllConcernStates} from './concern-projection.js';
import {eligibleQuestions} from './question-eligibility.js';
import {selectNextQuestion} from './question-scheduler.js';
import {needsTriage, buildTriageQuestion} from './triage.js';
import {stoppingDecision} from './sufficiency.js';
import {selectPlanConcerns} from './plan-priority.js';

export const DISCOVERY_VERSION='v3';
export function createDiscoverySession({concernIds=[],questionBank=[],labels={},outerGuardrail=14,facts={}}={}){return{version:DISCOVERY_VERSION,observationLog:[],facts:{...facts},concernIds:[...concernIds],questionBank,labels,asked:[],questionsAsked:0,outerGuardrail,phase:concernIds.length?'discovery':'gateway',triaged:false,incomplete:false,resolutionStates:{},driverKnown:{},recoveryAttempts:{},priorityResolutionUsed:false}}
export function activateConcerns(session,concernIds=[]){session.concernIds=[...new Set([...session.concernIds,...concernIds.filter(Boolean)])];for(const id of concernIds)if(!session.resolutionStates[id])session.resolutionStates[id]='triaged';session.phase='discovery';return session}
export function appendObservation(session,observation){session.observationLog=Object.freeze([...session.observationLog,observation]);if(observation.concernId&&!session.concernIds.includes(observation.concernId))activateConcerns(session,[observation.concernId]);return session}
export function mergeFacts(session,facts={}){session.facts={...(session.facts||{}),...facts};return session}
export function deriveStates(session){return deriveAllConcernStates(session.observationLog,session.concernIds).map(s=>({...s,resolutionState:session.resolutionStates?.[s.concernId]??'triaged',driverKnown:session.driverKnown?.[s.concernId]??false}))}
function gatewayQuestion(session){return session.questionBank.find(q=>q.role==='gateway'&&!session.asked.includes(q.id))}
function unresolved(states){return states.filter(s=>!s.excluded&&!['sufficient','resolved','deferred'].includes(s.resolutionState))}
function openConcernIds(states){return new Set(unresolved(states).map(s=>s.concernId))}
function questionTouchesOpenConcern(q,openIds){const ids=q.concernIds?.length?q.concernIds:[q.concernId].filter(Boolean);return !ids.length||ids.some(id=>openIds.has(id))}
function recoveryCandidates(session,states){const open=unresolved(states).sort((a,b)=>(b.safetyEscalationLevel??0)-(a.safetyEscalationLevel??0)||(b.memberImportanceRank??0)-(a.memberImportanceRank??0)||(b.evidenceConfidence??0)-(a.evidenceConfidence??0));for(const state of open){if((session.recoveryAttempts[state.concernId]??0)>=1)continue;const candidates=session.questionBank.filter(q=>q.role!=='gateway'&&!session.asked.includes(q.id)&&q.concernId===state.concernId&&(!q.prerequisite||q.prerequisite(state,session.observationLog)));const eligible=eligibleQuestions(candidates,[state],session.observationLog,session.facts||{}).sort((a,b)=>(b.specificityLevel??0)-(a.specificityLevel??0));if(eligible.length){session.recoveryAttempts[state.concernId]=(session.recoveryAttempts[state.concernId]??0)+1;return eligible}}return[]}
function redundantStateProbe(q,states){if(q.role!=='state-probe')return false;const ids=q.concernIds?.length?q.concernIds:[q.concernId].filter(Boolean);if(!ids.length)return false;return ids.every(id=>{const s=states.find(x=>x.concernId===id);return s&&Number(s.evidenceConfidence??0)>=.75&&(s.evidenceRefs?.length??0)>0})}
export function nextDiscoveryStep(session){if(session.phase==='gateway'&&session.concernIds.length===0){const q=gatewayQuestion(session);if(!q)return{type:'finish',stop:{reason:'no-gateway-question',incomplete:true},states:[]};session.asked=[...session.asked,q.id];session.questionsAsked++;return{type:'question',question:q,reason:'gateway',states:[]}}const states=deriveStates(session);const stop=stoppingDecision({states,questionsAsked:session.questionsAsked,outerGuardrail:session.outerGuardrail});if(stop.stop){session.incomplete=Boolean(stop.incomplete);return{type:'finish',stop,states}}if(!session.triaged&&needsTriage(session.concernIds))return{type:'triage',question:buildTriageQuestion(session.concernIds,session.labels),states};const openIds=openConcernIds(states);let candidates=eligibleQuestions(session.questionBank.filter(q=>q.role!=='gateway'&&!session.asked.includes(q.id)&&questionTouchesOpenConcern(q,openIds)&&!redundantStateProbe(q,states)),states,session.observationLog,session.facts||{}).map(q=>({...q,eligible:true})),recovery=false;if(!candidates.length){candidates=recoveryCandidates(session,states).filter(q=>!redundantStateProbe(q,states)).map(q=>({...q,eligible:true}));recovery=Boolean(candidates.length)}if(!candidates.length){session.incomplete=true;return{type:'finish',stop:{reason:'question-bank-exhausted',incomplete:true},states}}const recentQuestions=session.asked.map(id=>session.questionBank.find(q=>q.id===id)).filter(Boolean);const decision=selectNextQuestion({candidates,states,recentQuestions,facts:session.facts||{}});if(decision.type==='question'){session.asked=[...session.asked,decision.question.id];session.questionsAsked++;return{...decision,reason:recovery?'specificity-recovery':decision.reason,states}}session.incomplete=true;return{type:'finish',stop:{reason:'scheduler-exhausted',incomplete:true},states}}
export function markTriaged(session){session.triaged=true;return session}
export function setResolution(session,concernId,resolutionState,{driverKnown}={}){session.resolutionStates[concernId]=resolutionState;if(driverKnown!==undefined)session.driverKnown[concernId]=Boolean(driverKnown);return session}
export function recordPriorityResolution(session,concernIds=[]){session.priorityChoices=[...new Set(concernIds)].filter(id=>session.concernIds.includes(id)).slice(0,2);return session}
export function buildPlan(session,maxPlanSize=3){return selectPlanConcerns(deriveStates(session),maxPlanSize)}
