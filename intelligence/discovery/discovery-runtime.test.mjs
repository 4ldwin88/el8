import assert from 'node:assert/strict';
import * as discovery from './discovery-engine.js';
import { deriveConcernState } from './concern-projection.js';
import { makeObservation } from './contracts.js';
import { ROUND3_BANK, observationsForAnswer } from './question-bank-adapter.js';
import {createDiscoverySession,nextDiscoveryStep,discoveryOutput,discoveryPriorityCandidates,actionsForConfirmedPriorities} from '../../app/onboarding/discovery-runtime.js';
assert.equal(discovery.DISCOVERY_VERSION,'v3','canonical Discovery component version should be v3');
const runtime=discovery.session({concernIds:[]});assert.ok(runtime);assert.equal(runtime.version,'v3');assert.equal(typeof discovery.next(runtime),'object');assert.equal(typeof discovery.trace(runtime),'object');
const portSession=createDiscoverySession({concernIds:[]});assert.ok(portSession);assert.equal(typeof nextDiscoveryStep(portSession),'object');const proposal=discoveryOutput(portSession);assert.ok(proposal&&typeof proposal==='object');assert.ok('plan'in proposal);assert.ok('trace'in proposal);
const once=createDiscoverySession({concernIds:['money_pressure','physical_condition','poor_sleep','low_focus']});let step=nextDiscoveryStep(once);assert.equal(step.type,'triage');discovery.triage(once,{money_pressure:3,physical_condition:3,poor_sleep:1,low_focus:1});step=nextDiscoveryStep(once);assert.notEqual(step.type,'triage');assert.notEqual(step.type,'priority-resolution');
const emphasizedOutput={trace:{states:[{concernId:'money_pressure',resolutionState:'triaged',memberImportance:3,evidenceConfidence:0},{concernId:'physical_condition',resolutionState:'triaged',memberImportance:3,evidenceConfidence:0},{concernId:'poor_sleep',resolutionState:'triaged',memberImportance:1,evidenceConfidence:0},{concernId:'low_focus',resolutionState:'triaged',memberImportance:1,evidenceConfidence:0},{concernId:'low_energy',resolutionState:'triaged',memberImportance:null,evidenceConfidence:0}]},plan:{focus:[]}};const emphasized=discoveryPriorityCandidates(emphasizedOutput);assert.deepEqual(emphasized.map(x=>x.concernId),['money_pressure','physical_condition','low_focus','poor_sleep']);assert.equal(emphasized.some(x=>x.concernId==='low_energy'),false);
const inferred=discoveryPriorityCandidates({trace:{states:[{concernId:'low_energy',resolutionState:'triaged',memberImportance:null,evidenceConfidence:.45,evidenceRefs:['D2:inside']}]},plan:{focus:[]}});assert.equal(inferred.length,1);assert.equal(inferred[0].inferred,true);assert.deepEqual(inferred[0].evidenceRefs,['D2:inside']);
const multiDomainOutput={trace:{states:[{concernId:'money_pressure',label:'Money',resolutionState:'triaged',memberImportance:3,evidenceConfidence:.7,evidence:{severity:2,frequency:2,impact:2}},{concernId:'physical_condition',label:'Health',resolutionState:'triaged',memberImportance:3,evidenceConfidence:.7,evidence:{severity:2,frequency:2,impact:2}}]},plan:{focus:[]}};const multi=actionsForConfirmedPriorities(multiDomainOutput,['money_pressure','physical_condition'],{});assert.ok(multi.some(a=>a.concernId==='money_pressure'));assert.ok(multi.some(a=>a.concernId==='physical_condition'));
const fitObservation=makeObservation({id:'fit:1',questionId:'FIT1',concernId:'low_activity',effects:[{type:'feasibility',target:'low_activity',feasibility:{capacity:'low',scheduleFlexibility:'low'}},{type:'constraint',target:'low_activity',value:'limited_transport'},{type:'support',target:'low_activity',value:'partner_support'}]});const fitState=deriveConcernState([fitObservation],'low_activity');assert.equal(fitState.feasibility.values.capacity,'low');assert.equal(fitState.feasibility.values.scheduleFlexibility,'low');assert.deepEqual(fitState.feasibility.constraints,['limited_transport']);assert.deepEqual(fitState.feasibility.supports,['partner_support']);
const activityFitQuestion=ROUND3_BANK.find(q=>q.id==='PH1B'),activityFitObservations=observationsForAnswer(activityFitQuestion,['time','cost','mobility']),activityFitState=deriveConcernState(activityFitObservations,'energy');assert.equal(activityFitState.feasibility.values.scheduleFlexibility,'low');assert.equal(activityFitState.feasibility.values.costSensitivity,'high');assert.equal(activityFitState.feasibility.values.accessibilityNeeds,true);assert.ok(activityFitState.feasibility.constraints.includes('limited_time'));assert.ok(activityFitState.feasibility.constraints.includes('limited_budget'));assert.ok(activityFitState.feasibility.constraints.includes('mobility_accessibility'));
const v02Output={trace:{states:[{...activityFitState,concernId:'low_energy',label:'Energy',resolutionState:'triaged',memberImportance:3,evidenceConfidence:.8}]},plan:{focus:[]}},v02Actions=actionsForConfirmedPriorities(v02Output,['low_energy'],{signals:{feasibility:{time:'>30 min'},constraints:[]}});assert.ok(v02Actions.length>0);assert.deepEqual(v02Actions[0].feasibilityUsed,activityFitState.feasibility);assert.ok(v02Actions.every(a=>a.fitScore&&Number.isFinite(a.fitScore.total)));assert.ok(v02Actions.every(a=>a.fitScore.time<=4));
console.log('canonical Discovery v3 runtime + onboarding contract + feasibility regression tests passed');
