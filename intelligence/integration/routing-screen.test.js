import assert from'node:assert/strict';import{routeDimensions,routingEvidence,routingState}from'./routing-screen.js';assert.deepEqual(routeDimensions(['Physical','Financial']),['Physical','Financial']);assert.deepEqual(routeDimensions(['None right now']),[]);assert.equal(routingEvidence(['Physical'],'A lot').Physical,.68);assert.equal(routingState([], 'Moderately').healthy,true);assert.equal(routingState(['Spiritual'],'Severely').severity,.88);assert.equal(routingState([], 'Unsure').ambiguous,true);console.log('routing screen tests passed');