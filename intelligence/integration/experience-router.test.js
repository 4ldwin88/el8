import assert from'node:assert/strict';import{inferExperienceRoute,applyExperienceSeverity,shouldDiscover,expandRoute}from'./experience-router.js';let r=inferExperienceRoute(['work']);assert.equal(r.Occupational,.9);assert.equal(r.Financial,.35);assert(applyExperienceSeverity(r,'A lot').Occupational>applyExperienceSeverity(r,'A lot').Financial);assert.equal(shouldDiscover(['unsure'],{}),true);assert.equal(shouldDiscover(['multiple'],{}),true);assert(applyExperienceSeverity({Spiritual:.9},'Unsure').Spiritual>0);assert.equal(expandRoute({Emotional:.5},{'Financial.Security':.3}).Financial,.32);console.log('experience router tests passed');