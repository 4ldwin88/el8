import test from'node:test';import assert from'node:assert/strict';import{createMemberState}from'./member-state-contract.js';import{applyMemberStateTransition,MEMBER_STATE_EVENT as E}from'./member-state-transition.js';
const at='2026-09-02T18:00:00Z';
test('Member State records sparse evidence-backed engagement learning',()=>{let s=createMemberState({memberId:'T0001',now:at});s=applyMemberStateTransition(s,{type:E.ENGAGEMENT_SIGNAL_UPDATED,payload:{key:'burdenTolerance',value:'low',evidenceRefs:['review:r1'],sourceType:'review_learning',sourceRef:'r1',confidence:'moderate'},source:'review',at,expectedRevision:0});assert.equal(s.memberContext.engagementSignals.burdenTolerance.value,'low');assert.equal(s.memberContext.engagementSignals.burdenTolerance.sourceRef,'r1')});
test('Member State rejects unsupported fixed archetype labels',()=>{const s=createMemberState({memberId:'T0001',now:at});assert.throws(()=>applyMemberStateTransition(s,{type:E.ENGAGEMENT_SIGNAL_UPDATED,payload:{key:'archetype',value:'optimizer',sourceType:'qa',sourceRef:'x'},source:'qa',at,expectedRevision:0}),/unsupported engagement signal/)})
