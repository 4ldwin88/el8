// EL8 Intelligence Member State contract.
// Durable state records evidence and member decisions. Discovery, Prioritization,
// Planning, Safety and Review remain separate authorities.

import { DIMENSIONS, isDimensionId, isTopicId, isConstructId, CONSTRUCT_BY_ID, TAXONOMY_VERSION } from '../../registries/taxonomy/index.js';
import { CONSTRUCT_STATUS } from '../contracts/vocabulary.js';

export const CONDITION_STATUS = Object.freeze(['unknown', 'need_attention', 'stable', 'healthy', 'thriving']);
export const COVERAGE_STATUS = Object.freeze(['unknown', 'insufficient', 'sufficient']);
export const HYPOTHESIS_STATUS = Object.freeze(['generated', 'corroborating', 'contradicted', 'inconclusive', 'eligible_for_confirmation', 'member_confirmed', 'rejected', 'deferred']);
export const CONFIRMATION_STATUS = Object.freeze(['not_required', 'pending', 'confirmed', 'rejected', 'deferred']);
export const FACT_STATUS = Object.freeze(['current', 'superseded', 'retracted']);
export const FOCUS_DECISION = Object.freeze(['accepted', 'rejected', 'deferred', 'replaced']);
export const MEMBER_STATE_SCHEMA_VERSION = '3.1.0';
function isoNow() { return new Date().toISOString(); }
export function createMemberState({ memberId = null, now = isoNow() } = {}) {return {schemaVersion: MEMBER_STATE_SCHEMA_VERSION,taxonomyVersion: TAXONOMY_VERSION,memberId,revision: 0,createdAt: now,updatedAt: now,dimensions: Object.fromEntries(DIMENSIONS.map(({ id: dimensionId }) => [dimensionId, {dimensionId, conditionState: 'unknown', coverageState: 'unknown', coverageEvidenceRefs: [],topicIds: [], constructIds: [], strengthIds: [], constraintIds: [], evidenceRefs: [],lastObservedAt: null, lastDerivedAt: null,}])),constructs: {},facts: {},hypotheses: {},indicators: {},goals: {},constraints: {},focusDecisions: {},activeFocusIds: [],activePlanRef: null,activeActionIds: [],reviewCycles: [],memberContext: { preferences: {}, accessibility: {}, readiness: 'unknown', capacity: 'unknown', manageability: 'unknown' },safety: { dispositionRef: null, active: false, signalRefs: [], updatedAt: null },historyRefs: [],};}
export function createConstructState({ constructId, status = CONSTRUCT_STATUS.UNKNOWN, now = isoNow() } = {}) {if (!isConstructId(constructId)) throw new Error(`Unknown constructId: ${constructId}`);if (!Object.values(CONSTRUCT_STATUS).includes(status)) throw new Error(`Unknown construct status: ${status}`);const definition = CONSTRUCT_BY_ID[constructId];return {constructId,dimensionIds: [...definition.dimensionIds],topicIds: [...definition.topicIds],status,evidenceConfidence: 'unknown',sufficiency: 'insufficient',unresolvedReasons: [],factIds: [], evidenceRefs: [], observationRefs: [], indicatorIds: [], hypothesisIds: [],firstObservedAt: null, lastObservedAt: null, lastDerivedAt: now,};}
export function createFact({ factId, semanticKey, value = null, sourceType, sourceRef, affectedConstructId = null, affectedDimensionId = null, observedAt = isoNow(), timeWindow = null, reliability = 'unknown', memberConfirmed = false, currentStatus = 'current' } = {}) {if (!factId || !semanticKey || !sourceType || !sourceRef) throw new Error('factId, semanticKey, sourceType and sourceRef are required');if (affectedConstructId !== null && !isConstructId(affectedConstructId)) throw new Error(`Unknown constructId: ${affectedConstructId}`);if (affectedDimensionId !== null && !isDimensionId(affectedDimensionId)) throw new Error(`Unknown dimensionId: ${affectedDimensionId}`);if (!FACT_STATUS.includes(currentStatus)) throw new Error(`Unknown fact status: ${currentStatus}`);return { factId, semanticKey, value, sourceType, sourceRef, affectedConstructId, affectedDimensionId, observedAt, timeWindow, reliability, memberConfirmed, currentStatus };}
export function createHypothesis({ hypothesisId, proposition, linkedConstructIds = [], linkedDimensionIds = [], status = 'generated', confirmationStatus = 'not_required', now = isoNow() } = {}) {if (!hypothesisId || !proposition) throw new Error('hypothesisId and proposition are required');if (!HYPOTHESIS_STATUS.includes(status)) throw new Error(`Unknown hypothesis status: ${status}`);if (!CONFIRMATION_STATUS.includes(confirmationStatus)) throw new Error(`Unknown hypothesis confirmation: ${confirmationStatus}`);for (const id of linkedConstructIds) if (!isConstructId(id)) throw new Error(`Unknown constructId: ${id}`);for (const id of linkedDimensionIds) if (!isDimensionId(id)) throw new Error(`Unknown dimensionId: ${id}`);return { hypothesisId, proposition, linkedConstructIds: [...linkedConstructIds], linkedDimensionIds: [...linkedDimensionIds], evidenceFor: [], evidenceAgainst: [], status, confirmationStatus, createdAt: now, lastDerivedAt: now };}
export function createIndicator({ indicatorId, definition, constructId = null, dimensionId = null, unitOrScale = null, directionality = null } = {}) {if (!indicatorId || !definition) throw new Error('indicatorId and definition are required');if (constructId !== null && !isConstructId(constructId)) throw new Error(`Unknown constructId: ${constructId}`);if (dimensionId !== null && !isDimensionId(dimensionId)) throw new Error(`Unknown dimensionId: ${dimensionId}`);return { indicatorId, definition, constructId, dimensionId, unitOrScale, directionality, observations: [], trajectory: 'unknown' };}
export function createFocusDecision({ constructId, decision, decidedAt = isoNow(), reasonCodes = [], constraintRefs = [], replacementConstructId = null } = {}) {if (!isConstructId(constructId)) throw new Error(`Unknown constructId: ${constructId}`);if (!FOCUS_DECISION.includes(decision)) throw new Error(`Unknown focus decision: ${decision}`);if(decision==='replaced'&&!isConstructId(replacementConstructId))throw new Error('replaced Focus decision requires replacementConstructId');return { constructId, decision, decidedAt, reasonCodes: [...reasonCodes], constraintRefs: [...constraintRefs], replacementConstructId: decision==='replaced'?replacementConstructId:null };}
export function validateMemberStateShape(state) {const errors = [];if (!state || typeof state !== 'object') return ['state must be an object'];if (state.schemaVersion !== MEMBER_STATE_SCHEMA_VERSION) errors.push('unsupported schemaVersion');if (state.taxonomyVersion !== TAXONOMY_VERSION) errors.push('unsupported taxonomyVersion');if (!Number.isInteger(state.revision) || state.revision < 0) errors.push('revision must be a non-negative integer');for (const key of ['dimensions','constructs','facts','hypotheses','indicators','goals','constraints','focusDecisions']) if (!state[key] || typeof state[key] !== 'object') errors.push(`${key} must be an object`);for (const key of ['activeFocusIds','activeActionIds','reviewCycles','historyRefs']) if (!Array.isArray(state[key])) errors.push(`${key} must be an array`);for (const { id: dimensionId } of DIMENSIONS) {const d = state.dimensions?.[dimensionId];if (!d) { errors.push(`missing dimension: ${dimensionId}`); continue; }if (!CONDITION_STATUS.includes(d.conditionState)) errors.push(`invalid condition state: ${dimensionId}`);if (!COVERAGE_STATUS.includes(d.coverageState)) errors.push(`invalid coverage state: ${dimensionId}`);if (d.coverageState !== 'sufficient' && d.conditionState === 'stable' && (d.evidenceRefs?.length ?? 0) === 0) errors.push(`unassessed dimension cannot default stable: ${dimensionId}`);for (const topicId of d.topicIds ?? []) if (!isTopicId(topicId)) errors.push(`unknown topic ${topicId}: ${dimensionId}`);for (const constructId of d.constructIds ?? []) if (!isConstructId(constructId)) errors.push(`unknown construct ${constructId}: ${dimensionId}`);}for (const [constructId, item] of Object.entries(state.constructs ?? {})) {if (item.constructId !== constructId) errors.push(`construct key/id mismatch: ${constructId}`);if (!isConstructId(constructId)) errors.push(`unknown construct: ${constructId}`);if (!Object.values(CONSTRUCT_STATUS).includes(item.status)) errors.push(`invalid construct status: ${constructId}`);}for (const [factId, fact] of Object.entries(state.facts ?? {})) {if (fact.factId !== factId) errors.push(`fact key/id mismatch: ${factId}`);if (!fact.semanticKey || !fact.sourceType || !fact.sourceRef) errors.push(`fact missing provenance: ${factId}`);if (!FACT_STATUS.includes(fact.currentStatus)) errors.push(`invalid fact status: ${factId}`);}for (const [hypothesisId, h] of Object.entries(state.hypotheses ?? {})) {if (h.hypothesisId !== hypothesisId) errors.push(`hypothesis key/id mismatch: ${hypothesisId}`);if (!HYPOTHESIS_STATUS.includes(h.status)) errors.push(`invalid hypothesis status: ${hypothesisId}`);if (!CONFIRMATION_STATUS.includes(h.confirmationStatus)) errors.push(`invalid hypothesis confirmation: ${hypothesisId}`);if (h.status === 'member_confirmed' && h.confirmationStatus !== 'confirmed') errors.push(`confirmed hypothesis requires member confirmation: ${hypothesisId}`);}for(const [constructId,d] of Object.entries(state.focusDecisions??{})){if(!FOCUS_DECISION.includes(d.decision))errors.push(`invalid Focus decision: ${constructId}`);if(d.decision==='replaced'&&!isConstructId(d.replacementConstructId))errors.push(`replaced Focus missing replacement: ${constructId}`);}for (const constructId of state.activeFocusIds ?? []) {const decision = state.focusDecisions?.[constructId];if (!isConstructId(constructId)) errors.push(`active Focus references unknown construct: ${constructId}`);else if (!decision || decision.decision !== 'accepted') errors.push(`active Focus must have accepted member decision: ${constructId}`);}return errors;}
