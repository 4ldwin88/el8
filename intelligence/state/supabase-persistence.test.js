import test from'node:test';import assert from'node:assert/strict';import{createMemberState}from'./member-state-contract.js';import{applyMemberStateTransition,MEMBER_STATE_EVENT}from'./member-state-transition.js';import{loadMemberState,loadMemberStateForMigration,saveMemberState,loadOrCreateMemberState,assertPersistedMemberStateRow}from'./supabase-persistence.js';
const at='2026-08-30T20:00:00.000Z';function fakeClient(initial=null){let row=initial,rpcCalls=0;return{from(name){assert.equal(name,'el8_member_state');return{select(){return{maybeSingle:async()=>({data:row,error:null})}}}},async rpc(name,args){rpcCalls++;assert.equal(name,'save_el8_member_state');if(row&&Number(row.revision)!==args.expected_revision)return{data:null,error:new Error('member state revision conflict')};if(!row&&args.expected_revision!==-1)return{data:null,error:new Error('member state revision conflict')};row={schema_version:args.next_state.schemaVersion,revision:args.next_state.revision,state:structuredClone(args.next_state)};return{data:structuredClone(row),error:null}},row:()=>row,rpcCalls:()=>rpcCalls}}
test('first authenticated load creates v3 in existing JSON envelope',async()=>{const c=fakeClient(),s=await loadOrCreateMemberState(c,{memberId:'u1',now:at});assert.equal(s.schemaVersion,'3.0.0');assert.equal(c.row().schema_version,'3.0.0');assert.equal(c.row().revision,0)});
test('v3 transition round-trips without database shape change',async()=>{const c=fakeClient();let s=await loadOrCreateMemberState(c,{memberId:'u1',now:at});s=applyMemberStateTransition(s,{type:MEMBER_STATE_EVENT.CONSTRUCT_UPDATED,payload:{constructId:'SLEEP_QUALITY',status:'supported',evidenceRefs:['e1']},source:'qa',at,expectedRevision:s.revision});await saveMemberState(c,s,{expectedRevision:0});assert.deepEqual(await loadMemberState(c),s)});
test('strict canonical load rejects legacy schema row',()=>{const legacy={schema_version:'1.0.0',revision:0,state:{schemaVersion:'1.0.0',revision:0}};assert.throws(()=>assertPersistedMemberStateRow(legacy),/invalid canonical|schema mismatch/)});
test('migration load normalizes legacy row without writing it',async()=>{const legacyState={schemaVersion:'1.0.0',memberId:'u1',revision:4,createdAt:at,updatedAt:at,problems:[{id:'problem:poor_sleep',status:'SUPPORTED',evidenceRefs:['e1']}],priorities:[{id:'priority:poor_sleep',problemId:'problem:poor_sleep',status:'ACCEPTED',memberDecisionAt:at,evidenceRefs:['e1']}],engagementBurden:{capacity:null}};const c=fakeClient({schema_version:'1.0.0',revision:4,state:legacyState}),loaded=await loadMemberStateForMigration(c,{now:at});assert.equal(loaded.state.schemaVersion,'3.0.0');assert.equal(loaded.state.revision,4);assert.equal(loaded.state.constructs.SLEEP_QUALITY.status,'supported');assert.equal(loaded.state.focusDecisions.SLEEP_QUALITY.decision,'accepted');assert.equal(loaded.state.memberContext.capacity,'unknown');assert.equal(loaded.migration.required,true);assert.equal(loaded.migration.sourceRevision,4);assert.equal(loaded.migration.persisted,false);assert.equal(c.rpcCalls(),0);assert.equal(c.row().schema_version,'1.0.0')});
test('stale revision cannot overwrite durable truth',async()=>{const c=fakeClient(),s=await loadOrCreateMemberState(c,{memberId:'u1',now:at});const a=applyMemberStateTransition(s,{type:MEMBER_STATE_EVENT.MEMBER_CONTEXT_UPDATED,payload:{capacity:'low'},source:'qa',at,expectedRevision:0});await saveMemberState(c,a,{expectedRevision:0});const stale=applyMemberStateTransition(s,{type:MEMBER_STATE_EVENT.MEMBER_CONTEXT_UPDATED,payload:{capacity:'high'},source:'qa',at,expectedRevision:0});await assert.rejects(()=>saveMemberState(c,stale,{expectedRevision:0}),/revision conflict/)});
