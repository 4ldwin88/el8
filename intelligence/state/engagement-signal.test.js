import test from'node:test';import assert from'node:assert/strict';import{createEngagementSignal,isEngagementSignalDecisionUsable,validateEngagementSignals}from'./engagement-signal.js';
test('engagement signals require decision-useful provenance',()=>{const s=createEngagementSignal({key:'pacing',value:'small_steps',evidenceRefs:['review:r1'],sourceType:'review_learning',sourceRef:'r1',observedAt:'2026-09-01T00:00:00Z',confidence:'moderate'});assert.equal(s.key,'pacing');assert.deepEqual(validateEngagementSignals({pacing:s}),[]);assert.equal(isEngagementSignalDecisionUsable(s,{now:'2026-09-02T00:00:00Z'}),true)});
test('stale or retracted engagement signals are not decision-usable',()=>{const stale=createEngagementSignal({key:'burdenTolerance',value:'low',sourceType:'review_learning',sourceRef:'r1',observedAt:'2026-01-01T00:00:00Z',confidence:'moderate'}),retracted=createEngagementSignal({key:'burdenTolerance',value:'low',sourceType:'review_learning',sourceRef:'r2',observedAt:'2026-09-01T00:00:00Z',confidence:'high',status:'retracted'});assert.equal(isEngagementSignalDecisionUsable(stale,{now:'2026-09-02T00:00:00Z'}),false);assert.equal(isEngagementSignalDecisionUsable(retracted,{now:'2026-09-02T00:00:00Z'}),false)});
test('unsupported personality-style labels cannot enter the canonical signal map',()=>assert.throws(()=>createEngagementSignal({key:'archetype',value:'optimizer',sourceType:'qa',sourceRef:'x'}),/unsupported engagement signal/));
