name: Discovery Regression
on:
  push:
    paths:
      - 'intelligence/discovery/**'
      - 'app/onboarding/discovery-*.js'
      - 'package.json'
      - '.github/workflows/discovery-regression.yml'
  pull_request:
    paths:
      - 'intelligence/discovery/**'
      - 'app/onboarding/discovery-*.js'
      - 'package.json'
      - '.github/workflows/discovery-regression.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Canonical Discovery runtime and onboarding boundary
        run: npm run test:discovery
