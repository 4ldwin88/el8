name: Discovery Development Recovery Validation
on:
  push:
    branches: [feature/discovery-round-3-recovery]
  pull_request:
    paths:
      - 'development/discovery/**'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Architecture regressions
        run: node development/discovery/regression-tests.mjs
      - name: Adversarial end-to-end scenarios
        run: node development/discovery/end-to-end-tests.mjs
      - name: Human harness readiness
        run: node development/discovery/human-readiness-tests.mjs
