name: Discovery Development Validation
on:
  pull_request:
    branches: [main]
    paths:
      - 'development/discovery/**'
      - 'discovery-v2-6-question-bank.js'
      - '.github/workflows/discovery-round3-validation.yml'
  workflow_dispatch:
jobs:
  round3-validation:
    if: github.head_ref == 'feature/discovery-round-3' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Architecture regressions
        run: node development/discovery/regression-tests.mjs
      - name: Adversarial end-to-end scenarios
        run: node development/discovery/end-to-end-tests.mjs
