import {createMemberState,validateMemberStateShape} from '../../intelligence/state/member-state-contract.js';
import {loadMemberStateForMigration,saveMemberState} from '../../intelligence/state/supabase-persistence.js';
export function authenticatedMemberId(session){const id=session?.user?.id;if(!id)throw new Error('authenticated session required');return id}
function assertSessionState(state,memberId){const errors=validateMemberStateShape(state);if(errors.length)throw new Error(`invalid canonical Member State: ${errors.join('; ')}`);if(state.memberId!==memberId)throw new Error('session/member state identity mismatch');return state}
export async function openMemberStateSession({supabase,session,now=new Date().toISOString()}){const memberId=authenticatedMemberId(session),loaded=await loadMemberStateForMigration(supabase,{now});if(!loaded)return{memberId,state:createMemberState({memberId,now}),migration:null,persisted:false};assertSessionState(loaded.state,memberId);return{memberId,state:loaded.state,migration:loaded.migration,persisted:true}}
export async function persistMemberStateSession({supabase,session,previousState,nextState,migration=null,persisted=true}){const memberId=authenticatedMemberId(session);assertSessionState(previousState,memberId);assertSessionState(nextState,memberId);if(migration?.required)throw new Error('legacy Member State migration requires explicit migration commit');if(!Number.isInteger(previousState.revision)||!Number.isInteger(nextState.revision)||nextState.revision<=previousState.revision)throw new Error('session save requires canonical state advancement');if(!Array.isArray(nextState.historyRefs)||nextState.historyRefs.length<previousState.historyRefs.length)throw new Error('session save cannot remove canonical history references');return saveMemberState(supabase,nextState,{expectedRevision:persisted?previousState.revision:-1})}
