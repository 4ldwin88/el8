import{qaScenario,scenarioEvidence,assertExpectedScenario}from'./canonical-qa-scenarios.js';
import{applyQaReviewToBrowserState,summarizeBrowserMemberState}from'../../intelligence/member-state/browser-qa-adapter.mjs';
import{reviewPlan}from'../../intelligence/review/review-engine.js';
import{routeReview}from'../../intelligence/review/adaptation-router.js';
export function runCanonicalBrowserScenario({id,memberId,plan=null,state=null,now=new Date().toISOString()}){const scenario=qaScenario(id);if(!state)throw new Error('accelerated E2E QA requires canonical journey Member State');if(state.memberId!==memberId)throw new Error('QA Member State identity mismatch');const currentState=state;const reviewPlanShape=plan?.active?.length?plan:{status:'active',active:currentState.activePlan.interventions.map(i=>({id:i.id,driver:i.problemId,measurement:{},reviewRule:{}}))};if(!reviewPlanShape.active?.length)throw new Error('accelerated E2E QA requires an activated canonical plan');const evidence=scenarioEvidence(scenario);let review;if(id==='safety_hold')review={decision:'safety_hold',reason:'qa_safety_confirmation',evidenceUsed:{...evidence,evidenceRefs:['qa:safety']},pausePlan:true};else{const base=reviewPlan({plan:reviewPlanShape,evidence});review={...base,evidenceUsed:{...base.evidenceUsed,evidenceRefs:['qa:review']}}}const route=id==='safety_hold'?{route:'safety'}:routeReview({review,plan:reviewPlanShape});const assertion=assertExpectedScenario(scenario,review,route);const nextState=applyQaReviewToBrowserState(currentState,{scenario,review,route,at:now});return{scenario:scenario.label,pass:assertion.pass,errors:assertion.errors,evidence,reviewDecision:review.decision,reviewReason:review.reason,nextRoute:route.route,adaptationConstraint:route.adaptationConstraint||nextState.activePlan.adaptationRequest?.constraint||null,state:nextState,memberState:summarizeBrowserMemberState(nextState)}}
