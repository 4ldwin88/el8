import{qaScenario,scenarioEvidence,assertExpectedScenario}from'./qa-scenarios.js';
import{applyQaReviewToBrowserState,summarizeBrowserMemberState}from'../../intelligence/state/browser-qa-adapter.mjs';
import{reviewPlan}from'../../intelligence/review/review-engine.js';
import{routeReview,ADJUST_DISPOSITION}from'../../intelligence/review/adaptation-router.js';
function qaDecisionFor(id,route){if(id==='safety_hold')return'safety_hold';if(route.disposition===ADJUST_DISPOSITION.MAINTAIN)return'keep';if(route.disposition===ADJUST_DISPOSITION.REPLACE)return'replace';if(route.disposition===ADJUST_DISPOSITION.SIMPLIFY)return'simplify';if(route.disposition===ADJUST_DISPOSITION.PAUSE_REASSESS&&route.route==='discovery')return'reassess';if(route.route==='review-deepening')return'deepen';return'unknown'}
export function runBrowserScenario({id,memberId,plan=null,state=null,now=new Date().toISOString()}){const scenario=qaScenario(id);if(!state)throw new Error('accelerated E2E QA requires journey Member State');if(state.memberId!==memberId)throw new Error('QA Member State identity mismatch');const currentState=state;const reviewPlanShape=plan?.active?.length?plan:{status:'active',active:currentState.activeActionIds.map(actionId=>({id:actionId,driver:currentState.activeFocusIds[0]??null,measurement:{},reviewRule:{}}))};if(!reviewPlanShape.active?.length)throw new Error('accelerated E2E QA requires an activated plan');const evidence=scenarioEvidence(scenario);let review;if(id==='safety_hold')review={decision:'safety_hold',reason:'qa_safety_confirmation',evidenceUsed:{...evidence,evidenceRefs:['qa:safety']},pausePlan:true};else{const base=reviewPlan({plan:reviewPlanShape,evidence});review={...base,evidenceUsed:{...base.evidenceUsed,evidenceRefs:['qa:review']}}}const route=id==='safety_hold'?{route:'safety'}:routeReview({review,plan:reviewPlanShape});const reviewDecision=qaDecisionFor(id,route),qaReview={...review,decision:reviewDecision};const assertion=assertExpectedScenario(scenario,qaReview,route);const nextState=applyQaReviewToBrowserState(currentState,{scenario,review:qaReview,route,at:now});const summary=summarizeBrowserMemberState(nextState);return{scenario:scenario.label,pass:assertion.pass,errors:assertion.errors,evidence,reviewDecision,reviewReason:review.reason,nextRoute:route.route,adaptationConstraint:route.adaptationConstraint||summary.adaptationConstraint||null,state:nextState,memberState:summary}}
