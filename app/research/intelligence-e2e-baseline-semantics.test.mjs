import assert from'node:assert/strict';import fs from'node:fs';const projection=fs.readFileSync(new URL('../onboarding/member-state-projection.js',import.meta.url),'utf8');assert.match(projection,/baseline:\{status:'ESTABLISHED'/,'canonical Member State projection must establish baseline');const contract=fs.readFileSync(new URL('./intelligence-test-contract.js',import.meta.url),'utf8');assert.doesNotMatch(contract,/baseline:null/,'E2E session cannot model Baseline as a standalone process payload');console.log('Intelligence E2E baseline semantics regression passed');
