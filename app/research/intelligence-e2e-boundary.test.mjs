import assert from'node:assert/strict';import fs from'node:fs';const root=new URL('../../',import.meta.url),read=p=>fs.readFileSync(new URL(p,root),'utf8');const contract=read('app/research/intelligence-test-contract.js'),discovery=read('intelligence-test/discovery.html'),index=read('intelligence-test/index.html');assert.match(contract,/stage:'discovery'/);assert.doesNotMatch(contract,/stage:'baseline'/);assert.doesNotMatch(discovery,/initialPlanProposal/);assert.doesNotMatch(discovery,/baseline_handoff/);assert.match(index,/discovery-snapshot\.html/);console.log('Intelligence E2E canonical boundary regression passed');
