import assert from'node:assert/strict';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');for(const page of['index.html','discovery.html','priorities.html','selection-deepening.html','plan.html','activation-deepening.html','complete.html']){const file=path.join(root,'intelligence-test',page);assert.ok(fs.existsSync(file),`${page} must exist`);const html=fs.readFileSync(file,'utf8');for(const match of html.matchAll(/from['"](\.\.?\/[^'"]+)['"]/g)){const target=path.resolve(path.dirname(file),match[1].split('?')[0]);assert.ok(fs.existsSync(target),`${page} imports missing ${match[1]}`)}}assert.ok(!fs.existsSync(path.join(root,'intelligence-test','discovery-snapshot.html')),'retired discovery-snapshot.html must remain deleted');console.log('Intelligence E2E local browser import regression passed');
