import{INTELLIGENCE_TEST_VERSION,INTELLIGENCE_TEST_BUILD_VERSION,INTELLIGENCE_TEST_ENGINE_VERSION}from'./intelligence-test-version.js';
export const TESTER_MODES=Object.freeze(['self','roleplay','prefer_not_to_say']);
export function createIntelligenceTestSession({id=crypto.randomUUID(),testerMode='prefer_not_to_say',buildVersion=INTELLIGENCE_TEST_BUILD_VERSION}={}){if(!TESTER_MODES.includes(testerMode))throw new Error('Invalid tester mode');return{id,test_version:INTELLIGENCE_TEST_VERSION,build_version:buildVersion,engine_version:INTELLIGENCE_TEST_ENGINE_VERSION,tester_mode:testerMode,created_at:new Date().toISOString(),started_at:null,start_perf:null,sequence:0,stage:'introduction',baseline:null,discovery:null,recommended_priorities:[],confirmed_priorities:[],proposed_plan:null,priority_accuracy:null,survey:{}}}
export function beginIntelligenceTest(session,{now=new Date().toISOString(),perf=performance.now()}={}){return{...session,started_at:now,start_perf:perf,stage:'baseline'}}
export function elapsedMs(session,perf=performance.now()){return session?.start_perf==null?0:Math.max(0,Math.round(perf-session.start_perf))}
export function nextSequence(session){session.sequence=Number(session.sequence||0)+1;return session.sequence}
export function resetIntelligenceTestSession(options={}){return createIntelligenceTestSession(options)}
export function resultPayload(session){return{session_id:session.id,baseline:session.baseline||{},discovery:session.discovery||{},recommended_priorities:session.recommended_priorities||[],confirmed_priorities:session.confirmed_priorities||[],proposed_plan:session.proposed_plan||{},priority_accuracy:session.priority_accuracy,survey:session.survey||{},version_meta:{test_version:session.test_version,build_version:session.build_version,engine_version:session.engine_version},elapsed_ms:elapsedMs(session)}}
