// Browser presentation edge for canonical Planning.
// Planning input authority lives at the Focus-confirmation capability boundary; this adapter validates browser-provided canonical state and formats Plan output.
// Drive-governed no-autonomous-Action dispositions are not member-facing content defects, and Toolkit composition remains downstream of Action selection.
import {focusConfirmationToPlanning} from '../../intelligence/contracts/capability-boundaries.js';
import {composeToolkitForAction} from '../../intelligence/planning/toolkit-composition.js';
const clone=x=>structuredClone(x);
export function canonicalPlanningInputFromBrowser({memberStateRevision,confirmedFocus,evidenceRefs=[],constraintRefs=[],safetyDisposition='ordinary_flow',planningContext={}}={}){
 if(!Number.isInteger(memberStateRevision)||memberStateRevision<0)throw new Error('Member State revision is required');
 if(!Array.isArray(confirmedFocus))throw new Error('confirmedFocus must be an array');
 const focuses=confirmedFocus.map(f=>{if(!f?.constructId||f.decision!=='accepted')throw new Error('browser Planning accepts only member-accepted Focus');return clone(f)});
 return focusConfirmationToPlanning({memberStateRevision,focuses,evidenceRefs:[...evidenceRefs],constraintRefs:[...constraintRefs],safetyDisposition,planningContext});
}
export function canonicalBrowserPlanView(plan={}){const rawActions=plan.proposedActions||plan.actions||[],actions=rawActions.map(a=>({...clone(a),toolkit:a.toolkit??composeToolkitForAction(a.actionId||a.id)})),focusResolutions=clone(plan.focusResolutions||[]),governedNoAction=new Set(focusResolutions.filter(r=>r.noActionAllowed).map(r=>r.focusId)),uncovered=[...(plan.uncoveredFocusIds||[])].filter(id=>!governedNoAction.has(id));let status=plan.status||'no_plan',reason=plan.reason||null;if(status==='coverage_gap'&&!uncovered.length){if(actions.length){status='proposed';reason='member_confirmed_focus_with_governed_no_action_focus'}else{status='no_plan';reason='governed_no_autonomous_intervention'}}return{status,reason,focusIds:[...(plan.focusIds||[])],focusResolutions,deepeningRequirements:clone(plan.deepeningRequirements||[]),actions,backlog:clone(plan.backlog||[]),uncoveredFocusIds:uncovered,burden:clone(plan.burden||{}),trackingRequirements:actions.map(a=>a.trackingRequirement).filter(Boolean),additionalAssessmentRequirements:actions.map(a=>a.additionalAssessmentRequirement).filter(Boolean),activationStatus:status==='proposed'&&actions.length?'awaiting_member_acceptance':status==='deepen'?'needs_plan_specific_assessment':null,decisionTrace:clone(plan.decisionTrace||{})}}
