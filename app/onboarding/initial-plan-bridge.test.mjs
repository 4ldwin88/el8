import test from 'node:test';import assert from 'node:assert/strict';import{initialPlanFromBaseline}from'./initial-plan-bridge.js';
test('baseline can produce a deliberately basic first plan without full Discovery',()=>{const p=initialPlanFromBaseline({condition_baseline:{Financial:'Struggling',Physical:'Okay'},member_priority:'Financial',functional_impact:['Financial'],worsening:['Financial'],feasibility:{time:'5–15 min',overall_load:'Manageable'}});assert.equal(p.status,'proposed');assert.equal(p.confirmedPriorities[0],'money');assert.equal(p.candidateActions.length,1);assert.equal(p.requiresPlanSpecificAssessment,true);assert.match(p.explanation,/plan-specific/i)});
test('low capacity throttles first plan to one action',()=>{const p=initialPlanFromBaseline({condition_baseline:{Physical:'Needs attention'},member_priority:'Physical',feasibility:{time:'<5 min',overall_load:'Difficult'}});assert.equal(p.capacity.maxConcurrentActions,1);assert.equal(p.capacity.mode,'micro')});
test('unclear baseline asks for clarification instead of guessing',()=>{const p=initialPlanFromBaseline({condition_baseline:{Physical:'Okay',Emotional:'Okay'},member_priority:'No preference',functional_impact:['None'],worsening:['Unsure'],feasibility:{}});assert.equal(p.status,'needs_clarification');assert.equal(p.candidateActions.length,0)});
