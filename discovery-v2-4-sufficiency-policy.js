// Discovery v2.5 driver-closure policy.
const POLICY={energy:{scopeQuestion:'PH0',driverQuestion:'E2',targets:['low_energy','poor_sleep','low_activity','physical_condition','low_activation'],minTouches:3},work:{scopeQuestion:'W3',targets:['work_instability','schedule_disruption'],minTouches:2},relationships:{scopeQuestion:'R3',targets:['relationship_strain','low_support'],minTouches:2},support:{scopeQuestion:'S3',targets:['low_support','lonely','relationship_strain'],minTouches:2},focus:{scopeQuestion:'F2',targets:['low_focus','low_activation','low_energy','poor_sleep','stress','lack_direction','schedule_disruption'],minTouches:2},money:{driverQuestion:'M3',targets:['money_pressure','work_instability'],minTouches:1},sleep:{driverQuestion:'SL2',targets:['poor_sleep'],minTouches:2}};
const BROAD_CONCERNS=new Set(['energy','work','relationships','support','focus']);
const usable=a=>{if(!a)return null;const ids=(a.ids||[]).filter(id=>!['unsure','none','other'].includes(id));return ids.length?ids:null;},evidenceFor=(s,q)=>usable(s.answers.find(a=>a.qid===q));
function requirementFor(id){return POLICY[id]||null;}
function refreshSufficiency(s,signal){const r=requirementFor(signal.id);if(!r)return signal;const scope=r.scopeQuestion?evidenceFor(s,r.scopeQuestion):null,driver=r.driverQuestion?evidenceFor(s,r.driverQuestion):null;if(r.scopeQuestion)signal.scoped=!!scope;if(r.driverQuestion){signal.driverKnown=!!driver;signal.driverEvidence=driver?driver.join(','):null;}else if(r.scopeQuestion){signal.driverKnown=!!scope;signal.driverEvidence=scope?scope.join(','):null;}return signal;}
function priorityQuestion(signal,s){const r=requirementFor(signal.id);if(!r)return null;if(signal.id==='work'){const money=evidenceFor(s,'M3')||[];if(money.includes('no_income'))return null;}if(r.scopeQuestion&&!signal.scoped)return r.scopeQuestion;if(r.driverQuestion&&!signal.driverKnown)return r.driverQuestion;return null;}
export {POLICY,BROAD_CONCERNS,requirementFor,refreshSufficiency,priorityQuestion};export default {POLICY,BROAD_CONCERNS,requirementFor,refreshSufficiency,priorityQuestion};