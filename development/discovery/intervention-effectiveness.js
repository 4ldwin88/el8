export function makeInterventionOutcome({id,interventionId,concernId,intent,completed=false,intendedOutcomeAttained=null,magnitude=null,direction=null,timeToEffect=null,sustainability=null,memberPreference=null,timestamp=Date.now()}){if(!id||!interventionId||!concernId||!intent)throw new Error('Outcome requires id, interventionId, concernId and intent');return Object.freeze({id,interventionId,concernId,intent,completed:Boolean(completed),intendedOutcomeAttained,magnitude,direction,timeToEffect,sustainability,memberPreference,timestamp})}
export class InterventionOutcomeLog{#items=[];append(outcome){this.#items.push(Object.freeze({...outcome}));return outcome}all(){return this.#items.slice()}}
export function evaluateInterventionOutcome(intervention,outcome){if(!intervention||!outcome)throw new Error('Intervention and outcome required');if(intervention.intent==='learn')return{successful:Boolean(outcome.completed),criterion:'structured-follow-up-completed'};return{successful:outcome.intendedOutcomeAttained===true,criterion:'declared-expected-outcome'}}
export function learnOutcomeToObservationInput(intervention,structuredFollowUp){if(intervention.intent!=='learn')throw new Error('Only Learn interventions may produce follow-up observation input');if(!structuredFollowUp?.questionId||!structuredFollowUp?.concernId)throw new Error('Structured follow-up required');return{...structuredFollowUp,sourceType:'intervention-follow-up'} }
// Architectural boundary: this module intentionally has no import from contracts.js,
// observation-log.js, concern-projection.js, or any Discovery write/projection module.
// Outcomes have zero Discovery evidentiary weight unless a caller explicitly routes
// a structured Learn follow-up through the normal Observation pipeline.
