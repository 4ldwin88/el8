import{loadTestSession,event,note,clearTestSession}from'./intelligence-test.js';
const VERSION='Intelligence v0.2';
const stage=()=>document.body.dataset.testStage||'unknown';
const session=loadTestSession();
const style=document.createElement('style');style.textContent='.it-shell{display:flex;align-items:center;justify-content:space-between;gap:12px;margin:10px 0 18px;font-size:12px;color:var(--muted)}.it-exit{border:1px solid var(--line);background:var(--card);color:var(--ink);border-radius:10px;padding:7px 10px;font:inherit}.it-note{margin-top:20px}.it-note textarea{width:100%;min-height:82px;box-sizing:border-box;border:1px solid var(--line);background:var(--card);color:var(--ink);border-radius:12px;padding:12px;font:inherit}.it-note-msg{font-size:12px;color:var(--muted);margin-top:6px}';document.head.appendChild(style);
const main=document.querySelector('main');if(main){const shell=document.createElement('div');shell.className='it-shell';shell.innerHTML=`<strong>${VERSION}</strong>`;if(session?.started_at){const exit=document.createElement('button');exit.type='button';exit.className='it-exit';exit.textContent='Exit Test';exit.onclick=async()=>{await event(session,'test_exit',stage(),location.pathname.split('/').pop()||'unknown',null,{reason:'tester_exit'});clearTestSession();location.href='index.html'};shell.appendChild(exit)}main.insertBefore(shell,main.firstChild)}
if(session?.started_at&&main&&!document.getElementById('testerNote')){const card=document.createElement('section');card.className='el8-card it-note';card.innerHTML='<strong>Tester Notes</strong><p class="it-note-msg">Notice a bug, irrelevant or repeated question, confusing wording, missing option, or bad recommendation? Record it here.</p><textarea id="testerNote" placeholder="Optional note about this screen…"></textarea><button type="button" class="el8-btn" id="saveTesterNote" style="margin-top:8px">Save Note</button><div class="it-note-msg" id="testerNoteMessage"></div>';main.appendChild(card);card.querySelector('#saveTesterNote').onclick=async()=>{const t=card.querySelector('#testerNote').value.trim();if(!t)return;const ok=await note(session,{stage:stage(),screenId:location.pathname.split('/').pop()||'unknown',text:t});card.querySelector('#testerNoteMessage').textContent=ok?'Note saved.':'Note could not be saved.';if(ok)card.querySelector('#testerNote').value=''}}
